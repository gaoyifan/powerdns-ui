## PowerDNS primary/secondary deployment (Catalog Zones + TSIG) for this repo
##
## This file is consumed by:
## - `just` (via `set dotenv-path := ".env"`)
## - `docker compose` (automatically loads root .env)
##
## Keep this small and user-tweakable.

# Docker image
PDNS_IMAGE=powerdns/pdns-auth-50
PDNS_TAG=5.0.2

# Network Mode: leave empty for default (Compose bridge) or 'host'
# Use 'host' only if you need to map port 53 directly without Docker's userland proxy.
PDNS_NETWORK_MODE=

# PowerDNS API key for UI and pdns
PDNS_API_KEY=secret
API_UPSTREAM=

