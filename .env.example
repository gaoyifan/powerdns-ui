## PowerDNS primary/secondary deployment (Catalog Zones + TSIG) for this repo
##
## This file is consumed by:
## - `just` (via `set dotenv-path := ".env"`)
## - `docker compose` (automatically loads root .env)
##
## Keep this small and user-tweakable.

# Docker image
PDNS_IMAGE=powerdns/pdns-auth-50
PDNS_TAG=5.0.2

# Network (automatic bridge)

# Network Mode: 'bridge' (default) or 'host'
# Use 'host' for independent deployment to map port 53 directly
PDNS_PRIMARY_NETWORK_MODE=bridge
PDNS_SECONDARY_NETWORK_MODE=bridge

# Catalog Zones
PDNS_MEMBER_ZONE=example.com
PDNS_CATALOG_ZONE=catalog.pdns

# TSIG for signed NOTIFY + TSIG-authenticated AXFR/IXFR
PDNS_TSIG_NAME=pdns-catalog-tsig
PDNS_TSIG_ALGO=hmac-sha256
# Base64 secret. Replace for your environment if desired.
PDNS_TSIG_SECRET=0E8q4lXrWq4xH8bqHn3oZQqg1vD2oRzjQdVg6wQy9mQ=


